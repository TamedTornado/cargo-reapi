# Local real-world results — 2026-07-19

Host: Apple Silicon macOS, 8 logical CPUs, 20 GiB RAM, internal SSD plus an
external rotational qualification volume. Acceptance contract SHA-256:
`23112c312cbf0fab8bcb409522436934ebd099b33776d42cf0811b818f7f12a7`.

## Pinned Bevy 0.19 integrity fixture

The explicit fixture in `acceptance/bevy-fixture` builds both a linked Bevy
application and a linked integration-test binary. The successful run deleted
the producer, restored into a different-length consumer path, executed and
signature-checked both restored binaries, then deleted the restored target and
performed a physical cache-disabled control build under the same cargo-reapi
path-remapping ABI.

The run below used strict whole-gate execution through the externally installed
`@anthropic-ai/sandbox-runtime@0.0.66` provider (macOS Seatbelt), with the
provider package closure and policy included in the snapshot key.

| Measurement | Result |
| --- | ---: |
| Cold producer application build | 7m 03s |
| Cold producer test build | 3m 33s |
| Combined restored application + test gates | under the immutable 60s gate |
| Wrapper-observed consumer compile actions | 0 |
| Fresh cache-disabled control application build | 1m 21s |
| Fresh cache-disabled control test build | 1.44s |
| Whole proof wall time | 829.51s |
| Application behavior parity | pass |
| Test enumeration/stdout/stderr/status parity | pass |
| Consumer paths and macOS signatures | pass |

Command:

```sh
cargo test --test bevy \
  bevy_linked_artifact_restores_after_producer_deletion \
  -- --ignored --nocapture
```

This is a correctness and linked-artifact result. Its injected rustc observer
is independent of action classifications but is not the final OS-level
`eslogger` receipt required by aggregate acceptance.

## Moria rotational qualification

Stored report:
`/Volumes/Storage/cargo-reapi/rotational-clean-report-20260719`.

| Population | Measured | Fixed rotational limit | Result |
| --- | ---: | ---: | --- |
| One complete clean Moria gate | 245.764s | 300s | pass |
| Five simultaneous clean gates | 830.073s | 900s | pass |
| Ten simultaneous clean gates | 1,654.053s | 1,800s | pass |

The rotational run proves compatibility with the portable storage profile. It
does not substitute for SSD acceptance and predates the final aggregate receipt
schema.

## Bro five-job rotational qualification

Stored report: `/Volumes/Storage/cargo-reapi/bro-rotational-report-20260719`.
Five Bro `RustProjectGate` instances completed in 413.515s. Each member recorded
three whole-gate snapshot hits and zero wrapper-observed compile actions. This
also predates the mandatory `eslogger` receipt and is therefore qualification,
not final acceptance.

## SSD status

The previous SSD scratch run was explicitly invalid and has been deleted. A
fresh SSD Moria one/five/ten population plus the Bro five-job run remains
required under OS-level observation at the immutable 60/120/120-second clocks.
